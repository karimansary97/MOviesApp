export default Object.freeze({
  LISTING_DETAILS: 'ListingDetails',
});

/**
 * we could use object.freeze also to prevent that variable from change
 * and used it to give routes to screen
 * for more readabilty
 */
